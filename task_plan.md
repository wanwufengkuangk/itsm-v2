# ITSM V2 执行计划

## 目标与约束
独立重建 ITSM V2，不读取或复用 V1 产品和代码。以 Excel 为入口，不接数据库；技术基线 CAgent / Next.js 14 / Ant Design 5。

## 阶段
1. [完成] 读取用户提供的上下文，检查工作目录（空目录）。
2. [完成] 产品目标、指标、数据契约、交互与 UI 设计。
3. [完成] 独立分析核心及数据口径测试，初始 13 项通过。
4. [完成] 用户明确允许先使用独立 Next.js 14 + AntD 5 工程。
5. [完成] Excel 入口、全局筛选、运营总览、人员分析、诊断、趋势与导出已实现，待端到端验证。
6. [完成] 生产构建、类型检查、17项核心/Excel测试、2条浏览器端到端流程通过；桌面与手机截图已检查。

## 已解决的依赖与后续业务验证
- 用户明确允许暂不使用CAgent，独立Next.js14+AntD5已实现。
- imag为7张截图，非xlsx；真实数据分布、完整状态字典、评分尺度、SLA、复杂度仍待真实文件验证，产品不虚构阈值。

## 错误记录
- 首次构建：Next.js 遥测全局配置原子重命名出现 Windows EXDEV。通过项目启动脚本使用官方 NEXT_TELEMETRY_DISABLED=1 关闭非必要遥测，重新构建验证。
- 首屏样式：registry/cssinjs主版本不匹配，按AntD5依赖契约统一到registry1.1与cssinjs1.24，已验证。
- 测试定位：AntD隐藏测量行、图标可访问名、中文关闭按钮导致测试失败，依据实际DOM修正测试；无产品降级。
- ExcelJS在原生Node ESM命名空间下无Workbook命名导出，改为文档兼容的默认导出；浏览器上传及1904工作簿测试均通过。
